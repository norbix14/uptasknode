<!DOCTYPE html>
html(lang="es")
	head
		meta(charset="UTF-8")
		meta(name="viewport", content="width=device-width, initial-scale=1.0")
		style
			include ./email.css
	body
		h2 Reestablecer contraseña
		p Solicitaste reestablecer tu contraseña.
		p Click en el siguiente enlace para hacerlo.
		a(href=`${resetUrl}`) Reestablecer
		p Este enlace es temporal. Si se vence, solicita otro.
		p Si no solicitaste esto, puedes ignorarlo
