<!DOCTYPE html>
html(lang="es")
	head
		include partials/header
	body(class="login")
		.contenedor-formulario
			h1 Reestablecer contraseña
			p Colocar nueva contraseña
			form(class="caja-login" method="post")
				.campo
					label(for="password") Contraseña:
					input(type="password" 
								name="password"
					      placeholder="Tu nueva contraseña"
					      required)
				.campo.enviar
					input(type="submit" 
								class="boton" 
					      value="Reestablecer contraseña")
