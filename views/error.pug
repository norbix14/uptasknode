extends layout
block contenido
  .notfound
    h1 404 - Página no encontrada
    h2 Lo sentimos. Contenido no disponible
    if locals.environment === 'development'
      .deverror
        pre
          p= `${locals.environment} mode error`
          p= locals.message
          p= locals.error
